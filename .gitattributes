# Normalize all text files to LF in the repository
* text=auto eol=lf

*.ipynb diff=ipynb

# Common binaries (never normalize)
*.png  binary
*.jpg  binary
*.jpeg binary
*.pdf  binary
*.gif  binary
*.webp binary
